PS1="\W 🖖  "
git config --global user.email 30902737+kittysyddy@users.noreply.github.com
alias gco="git checkout"
alias gs="git status"
alias gcom="git commit -m "
alias gpush="git push origin "
alias ga="git add "
alias gpull="git pull origin "
alias gsquash='git rebase -i HEAD~`git rev-list --count HEAD ^master`'
export PATH="$PATH:$HOME/Library/Python/2.7/bin"
export PATH="$PATH:/usr/local/opt/terraform@0.11/bin"
# note: subl cli should be readily available already because the installation path is already in $PATH

# 🖖 which subl
# /usr/local/bin/subl
